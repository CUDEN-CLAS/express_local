#local_settings.py
#Values are 'local', 'dev', or 'prod'
environment = 'local'

#service identikey
ldap_user = '{{ LDAP_USER }}'
ldap_pw = '{{ LDAP_PASSWORD }}'
allowed_users = []

ssh_user = 'dplagnt'

# in case an environment needs a different url
api_server = 'http://inventory.local/inventory'
gsa_host = 'gsa.colorado.edu'
gsa_user = '{{ GSA_USER }}'
gsa_pw = '{{ GSA_PASSWORD }}'
