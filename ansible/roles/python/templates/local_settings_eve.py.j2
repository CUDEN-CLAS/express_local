#local_settings.py
#Values are 'local', 'dev', or 'prod'
environment = 'local'

#service identikey
ldap_user = '{{ LDAP_USER }}'
ldap_pw = '{{ LDAP_PASSWORD }}'
allowed_users = ['jabberbot']

ssh_user = 'dplagnt'

# in case an enviornment needs a different url
api_server = 'https://localhost/inventory'
gsa_host = 'gsa.colorado.edu'
gsa_user = '{{ GSA_USER }}'
gsa_pw = '{{ GSA_PASSWORD }}'
